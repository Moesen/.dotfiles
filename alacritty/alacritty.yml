import:
  - ~/.config/alacritty/colors.yml
  - ~/.config/alacritty/fonts.yml

window:
  history: 10000
  padding:
    x: 2
    y: 2
  dynamic_padding: true
  decorations: full

cursor:
  style:
    shape: Block
    blinking: On

env:
  TERM: xterm-256color

live_config_reload: true

import:
  - ~/.config/alacritty/colors.yml
  - ~/.config/alacritty/fonts.yml

window:
  history: 10000

cursor:
  style:
    shape: Block
    blinking: On

env:
  TERM: xterm-256color

live_config_reload: true
